apiVersion: v1
kind: Service
metadata:
  name: {{ include "callstack.fullname" . }}
  labels:
    {{- include "callstack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "callstack.selectorLabels" . | nindent 4 }}